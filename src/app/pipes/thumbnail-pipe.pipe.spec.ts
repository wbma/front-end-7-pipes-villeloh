import { ThumbnailPipePipe } from './thumbnail-pipe.pipe';

describe('ThumbnailPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ThumbnailPipePipe();
    expect(pipe).toBeTruthy();
  });
});
